import { assert } from "chai";
import { makeNegative } from "../../../Typescript/09-Return-negative/app";

describe("makeNegative", function () {
  it("Sample tests", function () {
    assert.equal(makeNegative(42), -42);
  });
});
